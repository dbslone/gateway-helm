apiVersion: v1
kind: Service
metadata:
  name: {{ include "bulwark.fullname" . }}
  labels:
    {{- include "bulwark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bulwark.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
